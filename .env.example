# Datenbank-Verbindung (MySQL)
DATABASE_URL="mysql://user:password@localhost:3306/cuer"

# Secret für NextAuth
NEXTAUTH_SECRET="CHANGEME_USE_a_strong_secret"

# URL der App (z. B. für Auth-Redirects)
NEXTAUTH_URL="http://localhost:3000"
